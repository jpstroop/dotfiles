# Machine-local overrides (not version controlled)
.zshrc.local
.zsh/*.local.zsh

# Secrets and credentials
*.key
*.pem
*.secret

# SSH — ignore everything except versioned config fragments and public keys
.ssh/*
!.ssh/config.d/
!.ssh/config.d/github
!.ssh/*.pub

# Shell history (machine-specific, potentially sensitive)
.zsh_history

# Generated by install script with machine-specific versions
.tool-versions

# macOS
.DS_Store
